"use client";
import { useState } from "react";
export function BookingForm(){const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(form:FormData){setBusy(true);setMessage("");const response=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({plate:form.get("plate"),make:form.get("make"),model:form.get("model"),date:form.get("date"),slot:form.get("slot")})});const data=await response.json();setBusy(false);setMessage(response.ok?"Prenotazione creata (#"+data.bookingId+"). Il pagamento test sarà disponibile al prossimo passaggio.":data.error||"Impossibile creare la prenotazione.");}return <form action={submit} className="panel form"><label>Marca<input name="make" required placeholder="Es. Fiat"/></label><label>Modello<input name="model" required placeholder="Es. 500"/></label><label>Targa<input name="plate" required placeholder="AB123CD"/></label><label>Data preferita<input name="date" type="date"/></label><label>Fascia oraria<select name="slot" defaultValue=""><option value="">Nessuna preferenza</option><option>09:00–12:00</option><option>14:00–18:00</option></select></label><p className="full">Prezzo della verifica: <b>€79,00</b>. Per prenotare devi prima accedere.</p><button className="button full" disabled={busy}>{busy?"Salvataggio…":"Continua al pagamento"}</button>{message&&<p className="notice full">{message}</p>}</form>}