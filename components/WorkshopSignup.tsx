"use client"; import { useState } from "react"; import { supabase } from "@/lib/supabase";
export function WorkshopSignup(){const [message,setMessage]=useState("");async function submit(form:FormData){if(!supabase)return setMessage("Configura Supabase.");const {data:{user}}=await supabase.auth.getUser();if(!user)return setMessage("Accedi prima di candidare l’officina.");const {error}=await supabase.from("workshops").insert({owner_auth_id:user.id,name:form.get("name"),vat_number:form.get("vat"),email:form.get("email"),phone:form.get("phone"),address:form.get("address"),city:form.get("city"),description:form.get("description")});setMessage(error?error.message:"Candidatura inviata. L’amministrazione la esaminerà.");}return <form action={submit} className="panel form"><label>Ragione sociale<input name="name" required/></label><label>Partita IVA<input name="vat" required/></label><label>Email<input name="email" type="email" required/></label><label>Telefono<input name="phone"/></label><label>Indirizzo<input name="address"/></label><label>Città<input name="city"/></label><label className="full">Presentazione<textarea name="description" rows={4}/></label><button className="button full">Invia candidatura</button>{message&&<p className="notice full">{message}</p>}</form>}