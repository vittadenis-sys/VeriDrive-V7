"use client";
import { useMemo,useState } from "react"; import Link from "next/link"; import { checklist } from "@/lib/checklist"; import { calculateVeriscore,scoreLabel } from "@/lib/veriscore";
export default function Checklist(){const [done,setDone]=useState<number[]>([]);const score=useMemo(()=>calculateVeriscore(checklist.map(i=>done.includes(i.id))),[done]);return <main className="page"><div className="shell"><Link href="/officina">← Torna alla dashboard</Link><div className="eyebrow" style={{marginTop:24}}>Verifica #VD-2026-0042</div><h1 style={{fontSize:42}}>Checklist Volkswagen Golf</h1><div className="panel"><p><b>VERISCORE live: {score}/100</b> · {scoreLabel(score)}</p><div className="checklist">{checklist.map(item=><label className="check" key={item.id}><input type="checkbox" checked={done.includes(item.id)} onChange={()=>setDone(d=>d.includes(item.id)?d.filter(x=>x!==item.id):[...d,item.id])}/><span><small>{item.id}. {item.area}</small><br/>{item.label}</span></label>)}</div><div className="actions"><Link className="button" href="/report/demo">Genera report</Link></div></div></div></main>}