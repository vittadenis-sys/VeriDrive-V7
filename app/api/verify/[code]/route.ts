import { NextResponse } from "next/server"; import { createServiceClient } from "@/lib/supabase/service";
export async function GET(_:Request,{params}:{params:Promise<{code:string}>}){try{const {code}=await params;const db=createServiceClient();const {data,error}=await db.from("certificates").select("public_code,is_revoked,issued_at,inspections(veriscore,bookings(plate,vehicle_make,vehicle_model))").eq("public_code",code).maybeSingle();if(error||!data)return NextResponse.json({valid:false},{status:404});return NextResponse.json({valid:!data.is_revoked,certificate:data});}catch{return NextResponse.json({valid:false,error:"Verifica non disponibile"},{status:503});}}