import { NextResponse } from "next/server"; import { requireAdmin } from "@/lib/authorization"; import { createServiceClient } from "@/lib/supabase/service";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;const {reason}=await request.json();const {error}=await createServiceClient().from("certificates").update({is_revoked:true,revoked_reason:reason||"Revocato dall’amministrazione"}).eq("id",id);if(error)throw error;return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Non autorizzato"},{status:401});}}